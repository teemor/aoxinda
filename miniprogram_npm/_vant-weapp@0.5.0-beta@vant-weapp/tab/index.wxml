<view
  wx:if="{{ animated || inited }}"
  class="custom-class van-tab__pane {{ active ? 'van-tab__pane--active' : 'van-tab__pane--inactive' }}"
  style="{{ animated || active ? '' : 'display: none;' }}"
>
  <slot />
</view>
