   <view class="space_store_img fl" bindtap="">
        <image src="{{item.img}}" />
      </view>
      <view class="fl space_store_text">
        <view>{{item.title}}</view>
        <view>
          <van-rate value="3" size="10" />
        </view>
        <view class="cl fz24 grey">
          <view class="fl">{{item.km}}</view>
          <view class="fr space_store_address">{{item.address}}</view>
          <view class="cl"></view>
        </view>
        <view wx:for="{{item.service}}" wx:key="{{item}}">
          <van-tag color="#999" plain class="fl" style="margin-right:20rpx">
            {{item}}
          </van-tag>
        </view>
        <view class="cl">
          <view class="fl" style="margin-top:8rpx">
            <van-tag type="primary">{{item.type}}</van-tag>
          </view>
          <view class="fl grey fz24" style="margin-top:12rpx;margin-left:10rpx">
            {{item.remark}}
          </view>
          <view class="fr red fz24" style="margin-top:10rpx">
            ¥{{item.price}}
          </view>
        </view>
      </view>
      <view class="cl"></view>
   